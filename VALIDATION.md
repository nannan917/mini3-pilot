## 1.0.2-mini3 修订（2026-09-08）

- 用户反馈：1.0.1 有图传及电量，但高度空白、拍照录像报 SDK 错误。
- 高度/位置改用官方公开的 KeyAircraftLocation3D；高度不依赖有效经纬度，0 米与负高度都可显示，无效值不会伪装为 0。
- 相机状态改为监听加主动读取；拍摄前读取录像状态，设置目标拍摄模式成功后再执行拍摄，所有相机 Key 显式使用 LEFT_OR_MAIN。
- 增加 SD 卡状态中文说明、错误日志及持久化保存；断开遥控器后仍能在连接诊断查看上次指令错误。
- 本机旧日志未保留相机错误详情，不能断言此次修订已解决实际拍摄故障；需要插卡状态与真机复测确认。
- 官方依据：[飞控三维位置](https://developer.dji.com/api-reference-v5/android-api/Components/IKeyManager/Key_FlightController_FlightControllerKey.html)、[相机模式及录像前置条件](https://developer.dji.com/api-reference-v5/android-api/Components/IKeyManager/Key_Camera_CameraKey.html)。

# 验证记录 · 2026-09-08

## 1.0.1 更新验证

- 版本 `1.0.1-mini3`，versionCode `21`：构建成功，7 项测试通过（4 项飞行状态条件、3 项遥测有效期判断），0 失败。
- APK 签名验证通过，与已安装的 1.0.0 版本一致，可覆盖升级。
- 修正只依赖高度回调刷新时间导致的静止状态误报；改用异步硬件读取确认关键数据，并分别记录有效期。
- 用户已反馈 1.0.0 接上 Mini 3 后能看到实时图传与电量，飞机静止时出现旧的“飞行数据未更新”提示。这是用户反馈，未通过电脑同步观察飞机链路。
- 1.0.1 已于 2026-09-08 11:38 覆盖安装到用户手机，确认 versionCode 21；冷启动约 0.5 秒，界面再次显示“注册成功 · 请连接 RC-N1”。静止状态下的遥测刷新仍待接回飞机复测；以下历史记录属于 1.0.0。

## 1.0.0 已完成

- `assembleDebug` 构建成功，版本 `1.0.0-mini3`，versionCode `20`，targetSdk `35`。
- 3 项 JVM 测试通过，0 失败：覆盖未知/过期/断连状态、起飞条件、返航点缺失及超时状态下的取消操作。
- APK 签名验证通过，与手机原调试安装包签名一致；已通过覆盖安装保留应用数据。
- 在用户的 vivo X90s（设备标识 V2241HA）、Android 16 上完成冷启动，启动耗时约 1.0 秒，进入首页。
- 运行库日志记录 `DJI protected library loaded`。本轮启动和注册观察中未重现原 V4 `DJISDKManager` 缺失异常。
- 使用用户提供的新 App Key，界面明确显示“注册成功 · 请连接 RC-N1”。
- 已查看本机截图，确认横屏布局、按钮、连接状态和未连接时的数据占位可见。
- 手机当前内存页大小为 4096 字节；本次启动失败不能据此归因于 16 KB 页大小。

## 待真机联调

USB 数据线接电脑时无法同时接 RC-N1，因此上述验证不包含飞机连接与视频链路。
Mini 3/RC-N1 的连接、图传、拍照录像、遥测正确性及起飞/返航/降落，需要接入真实飞行器后分别验证。
未进行任何自动起飞或实飞操作，不能将构建通过视为实飞验证通过。

## 版本范围

这是面向 Mini 3 的基础控制端，界面参考 DJI Fly，不是官方 DJI Fly 或其完整替代品。
不含完整地图、相册下载、官方账号/固件升级、全套相机参数或智能拍摄/航线功能。

1.0.2 本机验证：assembleDebug 成功；10 项 JVM 测试通过、0 失败；覆盖安装 Success，手机确认 versionCode=22、versionName=1.0.2-mini3；冷启动 Status=ok、510ms。高度及拍摄真机复测待完成。
